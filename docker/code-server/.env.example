# User/group IDs — run `id` on the host to get these values
PUID=1000
PGID=1000
# Host timezone
TZ=America/Sao_Paulo
# Web UI password
PASSWORD=changeme
# Optional sudo password inside container
SUDO_PASSWORD=changeme
