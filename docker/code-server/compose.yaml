# code-server
services:
  code-server:
    image: lscr.io/linuxserver/code-server:4.95.3
    container_name: code-server
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    volumes:
      - /home/${USER}/resources/docker/code-server/appdata:/config
      - /home/${USER}/.ssh:/config/.ssh:ro
      - /mnt/storage/codes:/codes
    ports:
      - 8443:8443
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - PASSWORD=${PASSWORD} #optional
      #- HASHED_PASSWORD= #optional
      - SUDO_PASSWORD=${SUDO_PASSWORD} #optional
      #- SUDO_PASSWORD_HASH= #optional
      #- PROXY_DOMAIN=code-server.my.domain #optional
      - DEFAULT_WORKSPACE=/config/workspace #optional
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8443"]
      interval: 30s
      timeout: 10s
      retries: 3


# TODO: Review if external network is still needed before uncommenting
# networks:
#   default:
#     external: true
#     name: nginx-default
